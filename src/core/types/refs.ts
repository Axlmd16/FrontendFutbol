export interface UserRef {
    id: number;
    full_name: string;
    dni: string;
}

export interface AthleteRef {
    id: number;
    full_name: string;
    dni: string;
}

export interface RepresentativeRef {
    id: number;
    full_name: string;
    dni: string;
}
